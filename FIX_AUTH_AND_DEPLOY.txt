================================================================================
FIX AUTHENTICATION AND DEPLOY
================================================================================

The error means old Qwiklabs credentials are cached. Follow these steps:

STEP 1: Clear Old Authentication
---------------------------------
gcloud auth revoke student-01-288349eb3323@qwiklabs.net


STEP 2: Login with New Account
-------------------------------
gcloud auth login

(Browser will open - log in with your NEW Google account for this project)


STEP 3: Set New Project
------------------------
gcloud config set project qwiklabs-gcp-00-4a7d408c735c


STEP 4: Set Application Default Credentials
--------------------------------------------
gcloud auth application-default login

(Browser will open again - authenticate)


STEP 5: Set Quota Project
--------------------------
gcloud auth application-default set-quota-project qwiklabs-gcp-00-4a7d408c735c


STEP 6: Verify Authentication
------------------------------
gcloud auth list
gcloud config get-value project


STEP 7: Enable APIs
--------------------
gcloud services enable cloudbuild.googleapis.com
gcloud services enable run.googleapis.com
gcloud services enable bigquery.googleapis.com


STEP 8: Deploy!
---------------
gcloud run deploy community-health-agent --source . --region us-central1 --allow-unauthenticated --set-env-vars GOOGLE_CLOUD_PROJECT=qwiklabs-gcp-00-4a7d408c735c,GOOGLE_API_KEY=AIzaSyAcq1AUFa-n4l_vmwtb3-DP1YpXzOj-zGM,GOOGLE_GENAI_USE_VERTEXAI=FALSE,BIGQUERY_DATASET=BQ_EPA_Air_Data --memory 2Gi --timeout 300


================================================================================
QUICK SUMMARY
================================================================================

The problem: Old Qwiklabs credentials (student-01-288349eb3323@qwiklabs.net) 
             are cached and invalid.

The fix: Login with NEW account, then deploy

Commands to run in order:
1. gcloud auth revoke student-01-288349eb3323@qwiklabs.net
2. gcloud auth login
3. gcloud config set project qwiklabs-gcp-00-4a7d408c735c
4. gcloud auth application-default login
5. gcloud auth application-default set-quota-project qwiklabs-gcp-00-4a7d408c735c
6. gcloud services enable cloudbuild.googleapis.com run.googleapis.com bigquery.googleapis.com
7. [Deploy command from Step 8 above]

================================================================================

