# Core Flask dependencies
Flask>=3.0.0
flask-cors>=6.0.0
gunicorn>=21.0.0
python-dotenv>=1.0.0

# Google Cloud & AI Dependencies
# IMPORTANT: Versions are pinned to prevent Cloud Build timeout (30min limit exceeded during pip resolve)
# DO NOT change these versions without testing Cloud Build deployment time
#
# Pub/Sub Integration (Added for async community report processing):
#   - google-cloud-pubsub==2.28.0: Pub/Sub client library for message publishing/subscribing
#
# Pinned Versions (Critical for Build Performance):
#   - google-adk==1.17.0: Agent Development Kit (pins transitive deps)
#   - google-cloud-aiplatform==1.121.0: Vertex AI platform (prevents version conflicts with ADK)
#   - google-cloud-storage==2.19.0: Cloud Storage (compatible with ADK/aiplatform versions)
#   - firebase-admin==6.9.0: Firebase SDK (prevents conflicts with google-auth)
#   - tenacity==8.5.0: Retry library (compatible with all Google Cloud libs)
#
# Flexible Versions (Can auto-resolve quickly):
#   - google-genai, google-auth, google-cloud-bigquery, google-cloud-texttospeech, 
#     google-generativeai: These resolve quickly with ">=" operator
#
google-adk==1.17.0
google-cloud-aiplatform==1.121.0
google-genai>=0.1.0
google-auth>=2.0.0
google-cloud-bigquery>=3.0.0
google-cloud-storage==2.19.0
google-cloud-texttospeech>=2.14.0
google-generativeai>=0.3.0
google-cloud-pubsub==2.28.0
firebase-admin==6.9.0
tenacity==8.5.0
db-dtypes>=1.1.0

# Data processing
# Data Processing
pandas>=1.5.0
PyPDF2>=3.0.0
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Export formats
openpyxl>=3.1.0
reportlab>=4.0.0
matplotlib>=3.7.0

# Utilities
requests>=2.31.0
feedparser>=6.0.0
xmltodict>=0.13.0

# US Location Data - lightweight, no complex dependencies
zipcodes>=1.2.0

# Firebase Authentication
firebase-admin>=6.0.0